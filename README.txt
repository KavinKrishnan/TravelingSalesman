To run the script, type the following:

python tsp_main.py -inst DATA/[File Name, e.g. Atlanta.tsp] -alg [Algorithm Name, e.g. BnB] -time [# of Seconds] -seed [Random Seed #]

The seed argument is optional

The output files will be in the folder 'output'

TSPBranchBound.py is not used
